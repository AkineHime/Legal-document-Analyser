<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <groupId>io.statigate</groupId>
    <artifactId>statigate-parent</artifactId>
    <version>0.1.0-SNAPSHOT</version>
    <packaging>pom</packaging>
    <name>statigate</name>
    <description>
        Statigate - offline, JVM-native AI legal document analyser for the Indian contract context.
        Single JVM process, zero network calls in the analysis pipeline. Every piece of generated
        advice is traceable to a source clause span and a specific Indian statute provision.
    </description>

    <modules>
        <module>core</module>
        <module>nlp</module>
        <module>ingestion</module>
        <module>extraction</module>
        <module>grounding</module>
        <module>advisory</module>
        <module>pipeline</module>
        <module>audit</module>
        <module>app</module>
    </modules>

    <properties>
        <maven.compiler.release>21</maven.compiler.release>
        <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
        <project.reporting.outputEncoding>UTF-8</project.reporting.outputEncoding>

        <!-- Dependency versions -->
        <tika.version>2.9.2</tika.version>
        <pdfbox.version>2.0.31</pdfbox.version>
        <opennlp.version>2.5.4</opennlp.version>
        <onnxruntime.version>1.20.0</onnxruntime.version>
        <jackson.version>2.18.1</jackson.version>
        <log4j.version>2.24.1</log4j.version>
        <slf4j.version>2.0.16</slf4j.version>
        <junit.version>5.11.3</junit.version>

        <jlama.version>0.8.4</jlama.version>
        <!-- Overridden per-OS/arch by os-maven-plugin below (e.g. linux-x86_64, osx-x86_64). -->
        <jlama.native.classifier>${os.detected.classifier}</jlama.native.classifier>

        <!-- Plugin versions -->
        <compiler.plugin.version>3.13.0</compiler.plugin.version>
        <surefire.plugin.version>3.5.2</surefire.plugin.version>
        <shade.plugin.version>3.6.0</shade.plugin.version>
        <os.plugin.version>1.7.1</os.plugin.version>
    </properties>

    <dependencyManagement>
        <dependencies>
            <dependency>
                <groupId>io.statigate</groupId>
                <artifactId>core</artifactId>
                <version>${project.version}</version>
            </dependency>
            <dependency>
                <groupId>io.statigate</groupId>
                <artifactId>nlp</artifactId>
                <version>${project.version}</version>
            </dependency>
            <dependency>
                <groupId>io.statigate</groupId>
                <artifactId>ingestion</artifactId>
                <version>${project.version}</version>
            </dependency>
            <dependency>
                <groupId>io.statigate</groupId>
                <artifactId>extraction</artifactId>
                <version>${project.version}</version>
            </dependency>
            <dependency>
                <groupId>io.statigate</groupId>
                <artifactId>grounding</artifactId>
                <version>${project.version}</version>
            </dependency>
            <dependency>
                <groupId>io.statigate</groupId>
                <artifactId>advisory</artifactId>
                <version>${project.version}</version>
            </dependency>
            <dependency>
                <groupId>io.statigate</groupId>
                <artifactId>pipeline</artifactId>
                <version>${project.version}</version>
            </dependency>
            <dependency>
                <groupId>io.statigate</groupId>
                <artifactId>audit</artifactId>
                <version>${project.version}</version>
            </dependency>

            <!-- Ingestion. Tika is scoped to just the parsers a contract tool needs (PDF, MS Office,
                 plain text) rather than the standard package, to cut both the attack surface and
                 the fat-JAR size. The PDF path uses PDFBox directly for page-offset tracking. -->
            <dependency>
                <groupId>org.apache.tika</groupId>
                <artifactId>tika-core</artifactId>
                <version>${tika.version}</version>
            </dependency>
            <dependency>
                <groupId>org.apache.tika</groupId>
                <artifactId>tika-parser-pdf-module</artifactId>
                <version>${tika.version}</version>
            </dependency>
            <dependency>
                <groupId>org.apache.tika</groupId>
                <artifactId>tika-parser-microsoft-module</artifactId>
                <version>${tika.version}</version>
            </dependency>
            <dependency>
                <groupId>org.apache.tika</groupId>
                <artifactId>tika-parser-text-module</artifactId>
                <version>${tika.version}</version>
            </dependency>
            <dependency>
                <groupId>org.apache.pdfbox</groupId>
                <artifactId>pdfbox</artifactId>
                <version>${pdfbox.version}</version>
            </dependency>

            <!-- Route third-party java.util.logging / Log4j2 (POI) through SLF4J. -->
            <dependency>
                <groupId>org.apache.logging.log4j</groupId>
                <artifactId>log4j-to-slf4j</artifactId>
                <version>${log4j.version}</version>
            </dependency>

            <!-- NLP -->
            <dependency>
                <groupId>org.apache.opennlp</groupId>
                <artifactId>opennlp-tools</artifactId>
                <version>${opennlp.version}</version>
            </dependency>

            <!-- ONNX Runtime (bundles win-x64 native libs) -->
            <dependency>
                <groupId>com.microsoft.onnxruntime</groupId>
                <artifactId>onnxruntime</artifactId>
                <version>${onnxruntime.version}</version>
            </dependency>
            <dependency>
                <groupId>com.fasterxml.jackson.core</groupId>
                <artifactId>jackson-databind</artifactId>
                <version>${jackson.version}</version>
            </dependency>

            <!-- Logging -->
            <dependency>
                <groupId>org.slf4j</groupId>
                <artifactId>slf4j-api</artifactId>
                <version>${slf4j.version}</version>
            </dependency>
            <dependency>
                <groupId>org.slf4j</groupId>
                <artifactId>slf4j-simple</artifactId>
                <version>${slf4j.version}</version>
            </dependency>

            <!-- Testing -->
            <dependency>
                <groupId>org.junit</groupId>
                <artifactId>junit-bom</artifactId>
                <version>${junit.version}</version>
                <type>pom</type>
                <scope>import</scope>
            </dependency>
        </dependencies>
    </dependencyManagement>

    <dependencies>
        <dependency>
            <groupId>org.junit.jupiter</groupId>
            <artifactId>junit-jupiter</artifactId>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>org.slf4j</groupId>
            <artifactId>slf4j-simple</artifactId>
            <scope>test</scope>
        </dependency>
    </dependencies>

    <build>
        <extensions>
            <!-- Sets ${os.detected.classifier} (e.g. windows-x86_64, linux-x86_64, osx-aarch64)
                 so the optional JLama native library resolves for the build host. -->
            <extension>
                <groupId>kr.motd.maven</groupId>
                <artifactId>os-maven-plugin</artifactId>
                <version>${os.plugin.version}</version>
            </extension>
        </extensions>
        <pluginManagement>
            <plugins>
                <plugin>
                    <groupId>org.apache.maven.plugins</groupId>
                    <artifactId>maven-compiler-plugin</artifactId>
                    <version>${compiler.plugin.version}</version>
                </plugin>
                <plugin>
                    <groupId>org.apache.maven.plugins</groupId>
                    <artifactId>maven-surefire-plugin</artifactId>
                    <version>${surefire.plugin.version}</version>
                    <configuration>
                        <!-- ONNX Runtime and JLama call System.load and use the Vector API; opt in
                             here so the JDK 21+ native-access warning does not clutter test output
                             and the optional JLama tests can load. JLama's FFM path additionally
                             needs preview mode, which the tester adds explicitly. -->
                        <argLine>-Xshare:auto --enable-native-access=ALL-UNNAMED --add-modules jdk.incubator.vector</argLine>
                        <systemPropertyVariables>
                            <org.slf4j.simpleLogger.defaultLogLevel>warn</org.slf4j.simpleLogger.defaultLogLevel>
                        </systemPropertyVariables>
                    </configuration>
                </plugin>
                <plugin>
                    <groupId>org.apache.maven.plugins</groupId>
                    <artifactId>maven-shade-plugin</artifactId>
                    <version>${shade.plugin.version}</version>
                </plugin>
            </plugins>
        </pluginManagement>
    </build>
</project>
